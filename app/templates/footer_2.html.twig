<footer>
    <div class="row">
        <div class="col-xs-8 text-left footer-content">
            {% if lang == 'en' %}
            <div lang="en" class="small-footer">
                *Subject to availability at each bar
            </div>
            {% endif %}
        </div>
        <div class="col-xs-4 text-right change-lang">
            <a lang="en" href="{{ url(app.request_stack.getCurrentRequest.get('_route'), {'_locale': 'en'}) }}">EN</a> /
            <a lang="cn" href="{{ url(app.request_stack.getCurrentRequest.get('_route'), {'_locale': 'cn'}) }}">中文</a>
        </div>
    </div>
</footer>